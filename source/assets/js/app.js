/* var options = {
	colorMode: 'grayscale',
	// compositeOperation: 'lighten',
	// iterationLimit: 0,
	// key: 'low',
	// lineWidth: 2,
	// lineMode: 'smooth',
	// origin: ['bottom'],
	// outputSize: 'original',
	// pathFinderCount: 30,
	speed: 20,
	// turningAngle: Math.PI
};

var imageElement = document.querySelector('.jgb');
var chromata = new Chromata(imageElement, options);
chromata.start();*/